# Convert to LaTeX

Convert to LaTeX is a simple Obsidian plugin that converts 
a Markdown note to a LaTeX document. Selecting the "Convert to LaTeX"
ribbon command will convert the current note to LaTeX. That LaTeX file 
can then be converted to PDF using a LaTeX compiler.
