# Convert to LaTeX

Convert to LaTeX is a simple Obsidian plugin that converts 
a Markdown note to a LaTeX document. Selecting the "Convert to LaTeX"
ribbon command will convert the current note to LaTeX. 
If automatic conversion to PDF is enabled the `.tex` file will
br converted to a PDF, but this requires a command line
LaTeX compiler such as `pdflatex`.
